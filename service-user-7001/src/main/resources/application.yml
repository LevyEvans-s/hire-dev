server:
  port: 7001
  tomcat:
    uri-encoding: utf-8
    max-swallow-size: -1
spring:
  profiles:
    active: dev
  banner:
    location: classpath:banner/banner.txt
logging:
  level:
    root: info
mybatis-plus:
  mapper-locations: classpath*:/mapper/*.xml
  global-config:
    db-config:
      id-type: assign_id
    banner: false